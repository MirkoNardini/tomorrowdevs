PI = 3.14
raggio = float(input("Inserisci raggio: "))

area = raggio * raggio * PI
volume = raggio * raggio * raggio * (PI/3)

print(" area uguale a: ", raggio, "e volume uguale a ", volume)

